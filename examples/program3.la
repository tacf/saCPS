f := proc(x,y){
    if(x) {
        ret(y)
    } else {
        x := g(2);
        if(y){
            x := 3;
            ret(x)
        } else {
            x := y;
            ret(x)
        }
    }
}

g := proc(w){
    z := w;
    z := 2;
    ret(z)
}